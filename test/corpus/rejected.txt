================================================
types :: A sized bound rejects a negative length
:error
================================================

-- The engine rejects this too:
--   Unexpected token `-`
LET $a: array<int, -3> = []

---

(ERROR)
==================================================
types :: A sized bound rejects a fractional length
:error
==================================================

-- The engine rejects this too:
--   Unexpected character `.` starting float, only integers are allowed here
LET $a: array<int, 1.5> = []

---

(ERROR)
==========================================
types :: A sized bound rejects a parameter
:error
==========================================

-- The engine rejects this too:
--   Unexpected token `a parameter`, expected an unsigned integer
LET $a: array<int, $n> = []

---

(ERROR)
=============================================
types :: A sized bound rejects a float suffix
:error
=============================================

-- The engine rejects this too:
--   Invalid token, found unexpected character `f after integer token
LET $a: array<int, 3f> = []

---

(ERROR)
==========================================
idioms :: A SET target rejects a parameter
:error
==========================================

-- The engine rejects this too:
--   Unexpected token `a parameter`, expected an identifier
CREATE t SET $x = 1

---

(ERROR)
====================================================
types :: A LET annotation rejects the bracketed form
:error
====================================================

-- The bracketed type is a closure-parameter spelling only. The engine
-- rejects it here too:
--   Unexpected token `<`, expected a kind name
LET $a: <int> = 2

---

(ERROR)
========================================================
types :: A function parameter rejects the bracketed form
:error
========================================================

-- Same rule inside a DEFINE FUNCTION argument list:
--   Unexpected token `<`, expected a kind name
DEFINE FUNCTION fn::g($x: <int>) { RETURN $x; }

---

(ERROR)
========================================================
clauses :: WITH INDEX takes identifiers, not a parameter
:error
========================================================

-- The engine rejects this too:
--   Unexpected token `a parameter`, expected an identifier
SELECT * FROM person WITH INDEX $p

---

(ERROR)
=============================================================
clauses :: WITH INDEX comes before the data clause, not after
:error
=============================================================

-- The engine rejects this too:
--   Unexpected token `WITH`, expected Eof
-- (On SELECT the modifier list is an order-free repeat inherited from
--  upstream, so the same misordering is still accepted there.)
UPDATE person SET a = 1 WITH INDEX idx

---

(ERROR)
==========================================
clauses :: The leading EXPLAIN has no FULL
:error
==========================================

-- FULL belongs to the trailing clause only. The engine rejects this:
--   Unexpected token `SELECT`, expected the query to end
EXPLAIN FULL SELECT * FROM person

---

(ERROR)
============================================
strings :: A string prefix is lowercase only
:error
============================================

-- `S'x'` is an identifier followed by a string. The engine says:
--   Unexpected token `a strand`, expected Eof
RETURN S'hello'

---

(ERROR)
==================================
info :: INFO FOR INDEX requires ON
:error
==================================

-- The engine rejects this too:
--   Unexpected token `;`, expected ON
INFO FOR INDEX idx

---

(ERROR)
======================================
info :: INFO FOR USER takes no VERSION
:error
======================================

-- VERSION is for the catalogue levels and a table, not a user. The engine:
--   Unexpected token `VERSION`, expected Eof
INFO FOR USER tobie VERSION d'2024-01-01T00:00:00Z'

---

(ERROR)
========================================
define :: DEFINE SEQUENCE has no COMMENT
:error
========================================

-- The engine rejects this too:
--   Unexpected token `COMMENT`, expected Eof
DEFINE SEQUENCE sq COMMENT 'x'

---

(ERROR)
===============================================
define :: DEFINE SEQUENCE's clauses are ordered
:error
===============================================

-- BATCH, then START, then TIMEOUT. The engine answers:
--   Unexpected token `BATCH`, expected Eof
DEFINE SEQUENCE sq START 5 BATCH 100

---

(ERROR)
===================================
remove :: REMOVE ACCESS requires ON
:error
===================================

-- The engine rejects this too:
--   Unexpected token `;`, expected ON
REMOVE ACCESS a

---

(ERROR)
=========================================
values :: A constant path is a closed set
:error
=========================================

-- The set is closed and the engine checks it while parsing:
--   Invalid function/constant path, did you maybe mean `math::SQRT_2`
RETURN math::SQRT2

---

(ERROR)
